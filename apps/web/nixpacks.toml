[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["cd ../.. && bun install"]

[phases.build]
cmds = ["cd ../.. && cd apps/web && bun run build"]

[start]
cmd = "cd apps/web && bun run preview --host 0.0.0.0 --port ${PORT:-3000}"
