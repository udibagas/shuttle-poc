[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = [
  "cd apps/api && bun run db:generate",
  "cd apps/api && bun run db:push",
  "cd apps/api && bun run build"
]

[start]
cmd = "cd apps/api && ./server"
